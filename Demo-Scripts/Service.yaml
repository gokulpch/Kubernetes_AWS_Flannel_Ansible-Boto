apiVersion: v1
kind: Service
metadata:
  namespace: kube-demo
  labels:
    run: nginx
  name: nginx
spec:
  ports:
  - nodePort: 31500
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: nginx
  type: NodePort
